import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mydentalclinic.app',
  appName: 'My Dental Clinic',
  webDir: 'dist'
};

export default config;
